 ${mine&&s.divisions.length?`<div class="order-count"><span>命令师数</span><input id="orderCount" type="number" min="1" max="${maxOrder}" value="${maxOrder}"></div><div class="actions"><button class="action" onclick="setOrder('move')"><strong>战略调动</strong><small>选择绿色相邻州</small></button><button class="action danger" onclick="setOrder('attack')"><strong>发起进攻</strong><small>选择红色相邻州</small></button></div>`:''}
 ${actionMode?`<div class="order-box">已进入“${actionMode.type==='move'?'调动':'进攻'}”模式：请在地图上选择一个高亮的相邻州。</div>`:''}<div class="section-title" style="margin-top:10px">相邻地区</div><div class="neighbors">${neighbors.map(n=>`<button class="neighbor" onclick="onStateClick('${n.id}')">${n.name} · ${G.countries[n.owner].short}</button>`).join('')}</div>`}
function divisionHTML(d){return `<div class="division"><div class="division-top"><b>${d.name}</b><span>${TEMPLATE[d.type].name}</span></div><div class="row"><span>兵力 ${Math.round(TEMPLATE[d.type].mp*d.strength/100/100)/10}K</span><span>装备 ${Math.round(d.equip)}%</span></div><div class="division-bars"><div class="tinybar"><i style="width:${d.strength}%"></i></div><div class="tinybar org"><i style="width:${d.org}%"></i></div></div></div>`}
function buildHere(type){if(queueConstruction(selectedState,type)){toast('已加入建设计划','good');renderAll()}else toast('无法在此建设','bad')}
function trainHere(type){if(queueDivision(selectedState,type)){toast('训练计划已建立','good');renderAll()}else toast('人力不足或州不受控制','bad')}
function setOrder(type){const count=clamp(Number($('#orderCount')?.value||1),1,G.states[selectedState].divisions.length);actionMode={type,source:selectedState,count};renderAll();toast(type==='move'?'选择相邻的己方州':'选择相邻的敌方州')}
function showBattle(r,name){showModal(`<h2>${r.won?'攻势成功':'进攻失败'}</h2><p>在${name}的战斗已经结束。</p><div class="cards"><div class="card"><h4>进攻战力</h4><b>${r.attack}</b></div><div class="card"><h4>防御战力</h4><b>${r.defense}</b></div><div class="card"><h4>敌军</h4><b>${r.defender}</b></div></div><div class="choices"><button class="choice" onclick="hideModal()"><b>返回地图</b><span>${r.won?'部队已进入目标州。':'补充装备、组织度和后勤后再战。'}</span></button></div>`)}
function openDrawer(type){$$('.nav').forEach(b=>b.classList.toggle('active',b.dataset.tab===type));if(type==='map'){closeDrawer();return}const d=$('#drawer');d.classList.remove('hidden');if(type==='production')drawProduction(d);if(type==='construction')drawConstruction(d);if(type==='research')drawResearch(d);if(type==='diplomacy')drawDiplomacy(d);if(type==='politics')drawPolitics(d);if(type==='log')drawLog(d)}
function closeDrawer(){$('#drawer').classList.add('hidden');$$('.nav').forEach(b=>b.classList.toggle('active',b.dataset.tab==='map'))}
const head=t=>`<div class="drawer-head"><h3>${t}</h3><button class="close" onclick="closeDrawer()">×</button></div>`;
function estimatedOutput(item,f){const c=G.countries[G.player],mult=(1+c.tech.industry*.08)*(c.economy==='war'?1.15:1)*(c.stability/100*.35+.65),base={rifle:680,artillery:28,tank:13,truck:90}[item];return Math.round(f*base*mult)}
function drawProduction(d){const c=G.countries[G.player],cap=productionCapacity(G.player),items={rifle:['步兵装备','支'],artillery:['火炮','门'],tank:['坦克','辆'],truck:['卡车','辆']};d.innerHTML=head('军工生产与装备库存')+`<div class="lawbox">军用工厂 ${cap.used}/${cap.mil} 已分配。每月结算产量，工厂分配可以随时调整。</div>${Object.entries(items).map(([id,[name,unit]])=>`<div class="production-row"><div class="prod-name"><b>${name}</b><small>预计月产 ${estimatedOutput(id,c.prod[id])} ${unit}</small></div><div class="allocator"><button onclick="alloc('${id}',-1)">−</button><b>${c.prod[id]}</b><button onclick="alloc('${id}',1)" ${cap.used>=cap.mil?'disabled':''}>＋</button></div><div class="stock">库存<br><b>${Math.round(c.stock[id]).toLocaleString()}</b></div></div>`).join('')}<div class="production-row"><div class="prod-name"><b>燃料</b><small>装甲与摩托化作战会消耗燃料</small></div><div></div><div class="stock"><b>${Math.round(c.stock.fuel).toLocaleString()}</b></div></div>`}
function alloc(id,delta){if(!changeProduction(id,delta))toast('没有空闲军用工厂','bad');drawProduction($('#drawer'));renderAll()}
function drawConstruction(d){const own=countryStates(G.player),active=own.filter(s=>s.build);d.innerHTML=head('国家建设计划')+`<div class="cards"><div class="card"><h4>可用民用工厂</h4><p>所有民用工厂共同推进现有项目。</p><div class="row"><span>总数</span><b>${totalFactories(G.player,'civ')}</b></div></div><div class="card"><h4>建设队列</h4><p>每个州同时只能进行一个项目。</p><div class="row"><span>进行中</span><b>${active.length}</b></div></div><div class="card"><h4>建设科技</h4><p>工程建设科技提高所有项目速度。</p><div class="row"><span>等级</span><b>${G.countries[G.player].tech.construction}</b></div></div></div><div style="margin-top:9px">${active.length?active.map(s=>`<div class="queue-row"><div class="row"><b>${s.name} · ${buildingName(s.build.type)}</b><span>${Math.round(s.build.progress)}/${s.build.cost}</span></div><div class="build-progress"><i style="width:${s.build.progress/s.build.cost*100}%"></i></div></div>`).join(''):'<div class="lawbox">没有进行中的项目。请在地图上选择己方州开始建设。</div>'}</div>`}
function drawResearch(d){const c=G.countries[G.player];d.innerHTML=head('国家科研')+`<div class="lawbox">科研能力：每月 ${c.research} 点。一次进行一个项目，科技最高 4 级。</div><div class="cards">${Object.entries(TECH).map(([id,t])=>{const lvl=c.tech[id],active=c.research===id,cost=t.cost+lvl*35;return `<div class="card"><h4>${t.name}</h4><p>${t.desc}</p><div class="row"><span>等级</span><b>${lvl}/4</b></div>${active?`<div class="row"><span>进度</span><b>${Math.round(c.researchProgress)}/${cost}</b></div><div class="build-progress"><i style="width:${c.researchProgress/cost*100}%"></i></div>`:`<button class="btn tiny" style="width:100%;margin-top:6px" ${c.research||lvl>=4?'disabled':''} onclick="research('${id}')">${lvl>=4?'已完成':'开始研究'}</button>`}</div>`}).join('')}</div>`}
function research(id){if(startResearch(G.player,id)){log(`开始研究：${TECH[id].name}。`,'good');drawResearch($('#drawer'));renderAll()}}
function drawDiplomacy(d){d.innerHTML=head('外交关系与战争')+Object.entries(G.countries).filter(([code,c])=>code!==G.player&&c.alive).map(([code,c])=>`<div class="diplo-row"><div class="diplo-dot" style="background:${c.color}"></div><div><div class="diplo-name">${c.name}</div><div class="diplo-meta">关系 ${Math.round(relation(G.player,code))} · ${atWar(G.player,code)?'交战中':G.pacts[pair(G.player,code)]?'互不侵犯条约':'和平'} · ${hasBorder(G.player,code)?'接壤':'不接壤'}</div></div><div class="diplo-actions"><button class="btn tiny" onclick="diploImprove('${code}')">改善关系</button>${atWar(G.player,code)?'<button class="btn tiny good" disabled>战争进行中</button>':`<button class="btn tiny" ${relation(G.player,code)<25||G.pacts[pair(G.player,code)]?'disabled':''} onclick="diploPact('${code}')">签约</button><button class="btn tiny danger" onclick="diploWar('${code}')">宣战</button>`}</div></div>`).join('')}
function diploImprove(code){if(improveRelations(code)){toast('外交关系改善','good');log(`与${G.countries[code].short}开展外交接触。`,'good')}else toast('政治点数不足','bad');drawDiplomacy($('#drawer'));renderAll()}
function diploPact(code){if(signPact(code)){toast('条约已经签署','good');log(`与${G.countries[code].short}签署互不侵犯条约。`,'good')}drawDiplomacy($('#drawer'))}
function diploWar(code){showModal(`<h2>对${G.countries[code].short}宣战？</h2><p>战争会立即开始。边境部队可以从相邻州发动进攻，装甲与摩托化部队会消耗燃料。</p><div class="choices"><button class="choice" onclick="confirmWar('${code}')"><b>正式宣战</b><span>关系降至 -100，双方战争支持上升。</span></button><button class="choice" onclick="hideModal()"><b>取消</b></button></div>`)}
function confirmWar(code){declareWar(G.player,code);hideModal();drawDiplomacy($('#drawer'));renderAll()}
function drawPolitics(d){const c=G.countries[G.player];d.innerHTML=head('政治、法律与动员')+`<div class="cards"><div class="card"><h4>政治点数</h4><p>外交和国家决策需要消耗政治点数。</p><div class="row"><span>当前</span><b>${Math.round(c.pp)}</b></div></div><div class="card"><h4>征兵制度</h4><p>提高动员等级会增加人力，但降低稳定度。</p><div class="row"><span>当前</span><b>${c.laws}</b></div></div><div class="card"><h4>经济制度</h4><p>战时经济提高军工产出，但降低民用建设效率。</p><div class="row"><span>当前</span><b>${c.economy==='war'?'战时经济':'民用经济'}</b></div></div></div><div style="margin-top:9px"><div class="decision"><div><b>扩大征兵</b><span>消耗 60 政治点；增加可用人力并降低稳定度。</span></div><button class="btn tiny" onclick="doMobilize()">执行</button></div><div class="decision"><div><b>转入战时经济</b><span>需要战争支持 50；消耗 70 政治点，军工产出 +15%。</span></div><button class="btn tiny" onclick="doWarEconomy()" ${c.economy==='war'?'disabled':''}>执行</button></div><div class="decision"><div><b>战争宣传</b><span>消耗 35 政治点，战争支持 +8%。</span></div><button class="btn tiny" onclick="doPropaganda()">执行</button></div></div>`}
function doMobilize(){if(mobilize()){toast('动员法令已生效','good');log('政府扩大了国家动员范围。','good')}else toast('政治点数不足或已达最高等级','bad');drawPolitics($('#drawer'));renderAll()}
function doWarEconomy(){if(warEconomy()){toast('已转入战时经济','good');log('国家经济转入战时体制。','good')}else toast('条件不足','bad');drawPolitics($('#drawer'));renderAll()}
function doPropaganda(){if(propaganda()){toast('战争宣传已经展开','good');log('政府开展全国战争宣传。','good')}else toast('政治点数不足','bad');drawPolitics($('#drawer'));renderAll()}
function drawLog(d){d.innerHTML=head('战报与历史日志')+`<div>${G.log.map(x=>`<div class="log-row ${x.type}"><time>${x.date}</time>${x.text}</div>`).join('')}</div>`}
function nextTurn(){endTurn();renderAll();toast('新月份开始','good')}
function showPendingEvent(){if(G.pendingEvent==='anschluss')showModal(`<h2>德奥合并</h2><p>柏林要求奥地利并入德意志国。接受会直接取得奥地利各州、部分人口与军队；拒绝将导致严重外交危机。</p><div class="choices"><button class="choice" onclick="eventChoice('accept')"><b>推动合并</b><span>吞并奥地利并接收其工业与部队。</span></button><button class="choice" onclick="eventChoice('reject')"><b>暂时退让</b><span>关系恶化，稳定度下降。</span></button></div>`)}
function eventChoice(c){resolvePendingEvent(c);hideModal();renderAll()}
function showHelp(first=false){if(first&&sessionStorage.getItem('ironV2Help'))return;sessionStorage.setItem('ironV2Help','1');showModal(`<h2>真实地理版玩法</h2><p>地图采用历史国家边界，并将各国划分为不规则战略州。每个数字单位代表一个师，兵力以千人为单位，装备和燃料采用实际数量级的缩放数据。</p><div class="choices"><div class="choice"><b>国家经营</b><span>分配军用工厂、建设民用/军用工厂、研究科技、修改征兵与经济制度。</span></div><div class="choice"><b>部队与补给</b><span>师拥有兵力、组织度和装备率。基础设施、铁路、地形、要塞和燃料影响作战。</span></div><div class="choice"><b>战争操作</b><span>选择有驻军的己方州，设置命令师数，再选择相邻州进行调动或进攻。</span></div><button class="choice" onclick="hideModal()"><b>进入游戏</b><span>双指缩放、单指拖动地图；建议 iPad 横屏。</span></button></div><p style="font-size:9px">历史边界底图基于 historical-basemaps 的 1930 GeoJSON；内部战略州为适配游戏而重构，并非逐一复刻行政区划。</p>`)}
function showModal(html){$('#modal').innerHTML=html;$('#modalBg').classList.remove('hidden')}
function hideModal(){$('#modalBg').classList.add('hidden')}
function toast(text,type=''){const t=$('#toast');t.textContent=text;t.className=`toast ${type}`;clearTimeout(window.toastTimer);window.toastTimer=setTimeout(()=>t.classList.add('hidden'),1900)}
$('#startBtn').onclick=startNew;$('#loadBtn').onclick=loadExisting;$('#endTurn').onclick=nextTurn;$('#saveBtn').onclick=()=>{saveGame(false);toast('游戏已保存','good')};$('#helpBtn').onclick=()=>showHelp(false);$('#menuBtn').onclick=returnMenu;$$('.nav').forEach(b=>b.onclick=()=>openDrawer(b.dataset.tab));$('#modalBg').onclick=e=>{if(e.target.id==='modalBg')hideModal()};window.addEventListener('resize',()=>{if(innerWidth>760)$('#right').classList.remove('open')});
loadMap();