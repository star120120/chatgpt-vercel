function annexCountry(a,b,reason){for(const s of countryStates(b)){s.owner=a;s.controller=a;for(const d of s.divisions)d.owner=a}G.countries[b].alive=false;G.countries[a].population+=G.countries[b].population*.72;G.countries[a].manpower+=G.countries[b].manpower*.45;log(`${reason}：${G.countries[b].short}被${G.countries[a].short}吞并。`,'bad')}
function resolvePendingEvent(choice){if(G.pendingEvent==='anschluss'){if(choice==='accept')annexCountry('GER','AUT','德奥合并');else{setRelation('GER','AUT',-85);G.countries.GER.war+=5;G.countries.GER.stability-=3;log('奥地利拒绝合并要求，中欧危机加剧。','bad')}}G.pendingEvent=null;}
function randomEvent(){if(Math.random()>.16)return;const c=G.countries[G.player],r=Math.random();if(r<.25){c.stock.rifle+=25000;log('军工企业提前完成一批步枪订单。','good')}else if(r<.5){c.stability=clamp(c.stability-3,0,100);log('国内劳资冲突导致稳定度下降。','bad')}else if(r<.75){c.pp+=18;log('政府改组提升了行政效率。','good')}else{c.manpower+=45;log('新一轮征兵登记增加了可用人力。','good')}}
function aiTurn(){for(const [code,c] of Object.entries(G.countries)){if(code===G.player||!c.alive)continue;aiResearch(code);aiBuild(code);aiTrain(code);aiDiplomacy(code);aiFront(code)}}
function aiResearch(code){const c=G.countries[code];if(!c.research){const ids=Object.keys(TECH).filter(x=>c.tech[x]<4);if(ids.length)startResearch(code,ids[Math.floor(Math.random()*ids.length)])}}
function aiBuild(code){const own=countryStates(code);if(!own.length)return;const active=own.filter(s=>s.build).length;if(active<Math.max(1,Math.floor(totalFactories(code,'civ')/5))&&Math.random()<.45){const s=own[Math.floor(Math.random()*own.length)];if(!s.build){const type=Math.random()<.55?'mil':Math.random()<.65?'infra':'fort';const costs={mil:110,infra:75,fort:65};s.build={type,progress:0,cost:costs[type]}}}}
function aiTrain(code){const c=G.countries[code];if(c.manpower<20||c.training.length>3||Math.random()>.38)return;const own=countryStates(code);if(!own.length)return;const type=c.stock.tank>400&&Math.random()<.16?'arm':c.stock.truck>3000&&Math.random()<.18?'mot':'inf';const t=TEMPLATE[type];if(c.manpower>=t.mp/1000){c.manpower-=t.mp/1000;c.training.push({stateId:own[Math.floor(Math.random()*own.length)].id,type,months:type==='arm'?5:type==='mot'?4:3,progress:0})}}
function aiDiplomacy(code){const c=G.countries[code];if(G.year<1937||Math.random()>.035*c.ai)return;const targets=Object.keys(G.countries).filter(o=>o!==code&&G.countries[o].alive&&!atWar(code,o)&&hasBorder(code,o)&&relation(code,o)<-25);if(targets.length)declareWar(code,targets[Math.floor(Math.random()*targets.length)])}
function aiFront(code){const wars=activeWars(code);if(!wars.length)return;const own=countryStates(code);for(const s of own){const border=stateNeighbors(s.id).some(n=>wars.includes(G.states[n].owner));if(border&&s.divisions.length<2){const rear=own.find(x=>x.divisions.length>3&&stateNeighbors(x.id).includes(s.id));if(rear){const d=rear.divisions.pop();s.divisions.push(d)}}}
 const attacks=[];for(const s of own)for(const n of stateNeighbors(s.id)){const t=G.states[n];if(wars.includes(t.owner)&&s.divisions.length){const atk=s.divisions.reduce((x,d)=>x+divisionPower(d,'attack',s),0),def=t.divisions.reduce((x,d)=>x+divisionPower(d,'defense',t),0)*(1+terrainDefense(t.terrain));if(atk>def*.8)attacks.push({from:s.id,to:n,ratio:atk/Math.max(1,def)})}}attacks.sort((a,b)=>b.ratio-a.ratio);if(attacks.length&&Math.random()<.7){const a=attacks[0];attackState(a.from,a.to,G.states[a.from].divisions.length,code)}}
function checkEnd(){if(!countryAlive(G.player)){G.gameOver=true;G.endReason='你的政府已经失去全部领土。'}else if(G.year>1945){G.gameOver=true;G.endReason=`战争时代结束。最终胜利点：${nationalSummary(G.player).victory}`}}
function saveGame(notify=true){if(G)localStorage.setItem('ironCrown1936V2',JSON.stringify(G));return notify;}
function loadGame(){const raw=localStorage.getItem('ironCrown1936V2');if(!raw)return null;try{G=JSON.parse(raw);return G}catch{return null}}
'use strict';
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const W=1200,H=800;let projection,pathGen,delaunay,voronoi,STATE_POS={},FEATURES={},selectedCountry='GER',selectedState=null,actionMode=null,mapReady=false;
function featureName(f){const p=f.properties||{};return String(p.NAME||p.name||p.Name||p.SOVEREIGNT||'');}
function detectCode(f){const n=featureName(f);for(const [code,re] of NAME_RULES)if(re.test(n))return code;return null;}
function geoVisible(f){try{const b=d3.geoBounds(f);return b[1][0]>-25&&b[0][0]<55&&b[1][1]>34&&b[0][1]<72}catch{return false}}
async function loadMap(){
 try{
  const res=await fetch(HISTORICAL_GEOJSON,{cache:'force-cache'});if(!res.ok)throw new Error('历史地图数据无法读取');const geo=await res.json();
  for(const f of geo.features){const code=detectCode(f);if(code&&COUNTRY[code]&&geoVisible(f)){(FEATURES[code]??=[]).push(f)}}
  projection=d3.geoMercator().center([14,52]).scale(650).translate([W/2,H/2-8]);pathGen=d3.geoPath(projection);
  buildCountryLayer();buildStateLayer();mapReady=true;$('#loading').classList.add('hidden');$('#start').classList.remove('hidden');renderCountryCards();
 }catch(e){$('#loading').innerHTML=`<div class="loader"><h1>地图载入失败</h1><p>${e.message}<br>请检查网络连接后刷新页面。</p></div>`}
}
function buildCountryLayer(){
 const defs=$('#map defs'),base=$('#countryBase'),borders=$('#historicBorders');
 for(const [code,features] of Object.entries(FEATURES)){
  const fc={type:'FeatureCollection',features};const clip=document.createElementNS('http://www.w3.org/2000/svg','clipPath');clip.id=`clip-${code}`;
  const cp=document.createElementNS('http://www.w3.org/2000/svg','path');cp.setAttribute('d',pathGen(fc));clip.appendChild(cp);defs.appendChild(clip);
  const p=document.createElementNS('http://www.w3.org/2000/svg','path');p.setAttribute('d',pathGen(fc));p.setAttribute('class','country-base');p.dataset.code=code;base.appendChild(p);
  const o=document.createElementNS('http://www.w3.org/2000/svg','path');o.setAttribute('d',pathGen(fc));o.setAttribute('class','historic-border');o.dataset.code=code;borders.appendChild(o);
 }
 const grat=d3.geoGraticule().extent([[-25,34],[55,72]]).step([5,5])();$('#graticule').setAttribute('d',pathGen(grat));
}
function buildStateLayer(){
 const pts=STATES.map(s=>{const p=projection([s.lon,s.lat]);STATE_POS[s.id]=p;return p});delaunay=d3.Delaunay.from(pts);voronoi=delaunay.voronoi([0,0,W,H]);
 const adj={};STATES.forEach((s,i)=>{let ns=[...delaunay.neighbors(i)].filter(j=>dist(pts[i],pts[j])<185).map(j=>STATES[j].id);const same=STATES.map((x,j)=>({j,d:dist(pts[i],pts[j])})).filter(x=>x.j!==i&&STATES[x.j].owner===s.owner).sort((a,b)=>a.d-b.d).slice(0,2).map(x=>STATES[x.j].id);ns=[...new Set([...ns,...same])];adj[s.id]=ns});
 const manual=[['sic','nap'],['nir','sco'],['lon','nor'],['sweng','bre'],['den','shl'],['pomr','epr'],['gmac','thr'],['pel','izm'],['norn','finn']];for(const [a,b] of manual){adj[a]??=[];adj[b]??=[];if(!adj[a].includes(b))adj[a].push(b);if(!adj[b].includes(a))adj[b].push(a)}setAdjacency(adj);
 const layer=$('#states'),labels=$('#labels'),units=$('#units');STATES.forEach((s,i)=>{
  const poly=voronoi.cellPolygon(i);if(!poly)return;const d='M'+poly.map(p=>p.join(',')).join('L')+'Z';const p=document.createElementNS('http://www.w3.org/2000/svg','path');p.setAttribute('d',d);if(FEATURES[s.owner])p.setAttribute('clip-path',`url(#clip-${s.owner})`);p.setAttribute('class','state');p.dataset.id=s.id;p.addEventListener('click',e=>{e.stopPropagation();onStateClick(s.id)});layer.appendChild(p);
  const [x,y]=pts[i];const t=document.createElementNS('http://www.w3.org/2000/svg','text');t.setAttribute('x',x);t.setAttribute('y',y-5);t.setAttribute('class','state-label');t.dataset.id=s.id;t.textContent=s.name;labels.appendChild(t);
  const g=document.createElementNS('http://www.w3.org/2000/svg','g');g.setAttribute('class','unit-counter');g.dataset.id=s.id;g.innerHTML=`<rect x="${x-13}" y="${y+2}" width="26" height="15" rx="3"></rect><text x="${x}" y="${y+13}">0</text>`;units.appendChild(g);
  if(s.capital){const star=document.createElementNS('http://www.w3.org/2000/svg','circle');star.setAttribute('cx',x+13);star.setAttribute('cy',y-11);star.setAttribute('r',3.8);star.setAttribute('class','capital');labels.appendChild(star)}
 });
 const zoom=d3.zoom().scaleExtent([.75,4]).on('zoom',ev=>$('#mapWorld').setAttribute('transform',ev.transform));d3.select('#map').call(zoom).on('dblclick.zoom',null);$('#map').addEventListener('click',()=>{if(innerWidth<=760)$('#right').classList.remove('open')});
}
const dist=(a,b)=>Math.hypot(a[0]-b[0],a[1]-b[1]);
function renderCountryCards(){const playable=['GER','FRA','UK','USSR','ITA','POL','ESP','TUR'];$('#countryGrid').innerHTML=playable.map(code=>{const c=COUNTRY[code];return `<button class="country-card ${selectedCountry===code?'active':''}" style="--c:${c.color}" data-code="${code}"><b>${c.short}</b><span><em>${c.ideology}</em><br>人口 ${c.population.toFixed(1)}M · 初始陆军约 ${Object.values(c.army).reduce((a,b)=>a+b,0)} 个师<br>${code==='GER'?'工业与快速扩张':code==='USSR'?'庞大纵深与人力':code==='UK'?'海权、科技与有限陆军':code==='FRA'?'防御、工业与联盟':code==='ITA'?'地中海扩张':code==='POL'?'两线压力与生存':code==='ESP'?'内政危机与重建':'区域平衡与海峡'}</span></button>`}).join('');$$('.country-card').forEach(b=>b.onclick=()=>{selectedCountry=b.dataset.code;renderCountryCards()})}
function startNew(){if(!mapReady)return;createGame(selectedCountry,$('#difficulty').value);selectedState=countryStates(G.player).find(s=>s.capital)?.id||countryStates(G.player)[0]?.id;showGame();}
function loadExisting(){if(!loadGame()){toast('没有找到 V2 存档','bad');return}selectedCountry=G.player;selectedState=countryStates(G.player).find(s=>s.capital)?.id||countryStates(G.player)[0]?.id;showGame();toast('已读取存档','good')}
function showGame(){$('#start').classList.add('hidden');$('#game').classList.remove('hidden');actionMode=null;renderAll();showHelp(true)}
function returnMenu(){saveGame(false);$('#game').classList.add('hidden');$('#start').classList.remove('hidden');closeDrawer()}
function renderAll(){if(!G)return;renderTop();renderLeft();renderMap();renderInspector();if(G.pendingEvent)showPendingEvent();if(G.gameOver)showModal(`<h2>战局结束</h2><p>${G.endReason}</p><div class="choices"><button class="choice" onclick="returnMenu();hideModal()"><b>返回主菜单</b></button></div>`)}
function renderTop(){const c=G.countries[G.player],n=nationalSummary(G.player);$('#topNation').textContent=c.short;$('#sManpower').textContent=`${Math.round(c.manpower)}K`;$('#sCiv').textContent=n.civ;$('#sMil').textContent=n.mil;$('#sDiv').textContent=n.divisions;$('#sPP').textContent=Math.round(c.pp);$('#sFuel').textContent=`${Math.round(c.stock.fuel/1000)}K`;$('#date').innerHTML=`${G.year}年${G.month}月<small>第 ${G.turn} 回合</small>`}
function renderLeft(){const c=G.countries[G.player],n=nationalSummary(G.player);$('#nationDot').style.background=c.color;$('#nationName').textContent=c.name;$('#nationIdeology').textContent=c.ideology;$('#stabText').textContent=`${Math.round(c.stability)}%`;$('#warText').textContent=`${Math.round(c.war)}%`;$('#stabBar').style.width=`${c.stability}%`;$('#warBar').style.width=`${c.war}%`;$('#overview').innerHTML=`<div class="mini"><small>总人口</small><b>${c.population.toFixed(1)}M</b></div><div class="mini"><small>现役兵力</small><b>${n.armyManpower}K</b></div><div class="mini"><small>控制州</small><b>${n.states}</b></div><div class="mini"><small>胜利点</small><b>${n.victory}</b></div>`;$('#lawbox').innerHTML=`征兵法：<b>${c.laws}</b><br>经济法：<b>${c.economy==='war'?'战时经济':'民用经济'}</b><br>科研：<b>${c.research?TECH[c.research].name:'空闲'}</b>`}
function renderMap(){
 $$('.state').forEach(p=>{const s=G.states[p.dataset.id];p.style.fill=G.countries[s.owner]?.color||'#555';p.style.opacity=s.damage?String(1-s.damage/200):'1';p.classList.toggle('selected',p.dataset.id===selectedState);p.classList.remove('target-friendly','target-hostile');if(actionMode&&stateNeighbors(actionMode.source).includes(s.id)){if(actionMode.type==='move'&&s.owner===G.player)p.classList.add('target-friendly');if(actionMode.type==='attack'&&s.owner!==G.player)p.classList.add('target-hostile')}});
 $$('.unit-counter').forEach(g=>{const s=G.states[g.dataset.id],n=s.divisions.length;g.style.display=n?'':'none';g.querySelector('text').textContent=n;g.querySelector('rect').style.stroke=G.countries[s.owner]?.color==='#727b86'?'#f0d17c':'#e7edf4'});
}
function onStateClick(id){
 if(actionMode&&id!==actionMode.source){if(!stateNeighbors(actionMode.source).includes(id)){toast('只能选择相邻州','bad');return}const src=G.states[actionMode.source],dst=G.states[id];if(actionMode.type==='move'){if(dst.owner!==G.player){toast('调动目标必须由我方控制','bad');return}moveDivisions(src.id,id,actionMode.count);selectedState=id;actionMode=null;renderAll();return}if(actionMode.type==='attack'){if(dst.owner===G.player){toast('进攻目标必须是敌方州','bad');return}if(!atWar(G.player,dst.owner)){toast(`尚未与${G.countries[dst.owner].short}交战`,'bad');openDrawer('diplomacy');actionMode=null;return}const r=attackState(src.id,id,actionMode.count);actionMode=null;selectedState=r?.won?id:src.id;renderAll();if(r)showBattle(r,dst.name);return}}
 selectedState=id;actionMode=null;renderAll();if(innerWidth<=760)$('#right').classList.add('open');
}
function supplyClass(r){return r>.8?'good':r>.5?'warn':'bad'}
function renderInspector(){const host=$('#inspector');if(!selectedState||!G.states[selectedState]){host.innerHTML='<div class="state-head"><h2>选择一个州</h2><p>在地图上点选区域。</p></div>';return}const s=G.states[selectedState],c=G.countries[s.owner],mine=s.owner===G.player,ratio=stateSupplyRatio(s),neighbors=stateNeighbors(s.id).map(id=>G.states[id]);const maxOrder=Math.max(1,s.divisions.length);host.innerHTML=`<div class="state-head"><h2>${s.name}${s.capital?' ★':''}</h2><p>${c.name} · ${s.terrain} · 人口 ${s.pop.toFixed(1)}M</p><div class="state-stats"><div><small>民用/军用工厂</small><b>${s.civ} / ${s.mil}</b></div><div><small>基础设施/铁路</small><b>${s.infra} / ${s.rail}</b></div><div><small>补给状态</small><b class="supply ${supplyClass(ratio)}">${Math.round(ratio*100)}%</b></div><div><small>要塞</small><b>${s.fort}</b></div><div><small>资源</small><b>${s.res||'—'}</b></div><div><small>驻军</small><b>${s.divisions.length} 师</b></div></div>${s.build?`<p>建设中：${buildingName(s.build.type)} ${Math.round(s.build.progress)}/${s.build.cost}</p><div class="build-progress"><i style="width:${s.build.progress/s.build.cost*100}%"></i></div>`:''}</div>
 <div class="section-title" style="margin-top:10px">驻军编成</div><div class="division-list">${s.divisions.length?s.divisions.slice(0,10).map(d=>divisionHTML(d)).join(''):'<div class="lawbox">本州没有正规师。</div>'}${s.divisions.length>10?`<div class="lawbox">另有 ${s.divisions.length-10} 个师未展开显示。</div>`:''}</div>
 <div class="section-title" style="margin-top:10px">州级行动</div><div class="actions"><button class="action" ${mine&& !s.build?'':'disabled'} onclick="buildHere('civ')"><strong>民用工厂</strong><small>建设成本 120</small></button><button class="action" ${mine&&!s.build?'':'disabled'} onclick="buildHere('mil')"><strong>军用工厂</strong><small>建设成本 110</small></button><button class="action" ${mine&&!s.build&&s.infra<10?'':'disabled'} onclick="buildHere('infra')"><strong>基础设施</strong><small>改善补给与建设</small></button><button class="action" ${mine&&!s.build&&s.fort<5?'':'disabled'} onclick="buildHere('fort')"><strong>修筑要塞</strong><small>防御加成 +12%</small></button><button class="action" ${mine?'':'disabled'} onclick="trainHere('inf')"><strong>训练步兵师</strong><small>12K 人力 · 3月</small></button><button class="action" ${mine?'':'disabled'} onclick="trainHere('arm')"><strong>训练装甲师</strong><small>10K 人力 · 5月</small></button></div>
 