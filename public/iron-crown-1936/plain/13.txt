Pact(code){if(relation(game.player,code)<25){toast('关系至少需要达到 25','bad');return}game.pacts[pairKey(game.player,code)]=true;setRelation(game.player,code,relation(game.player,code)+8);log(`与${game.countries[code].short}签署互不侵犯条约。`,'good');renderDiplomacy(document.getElementById('drawer'))}
function declareWar(a,b,playerInitiated=false){if(isAtWar(a,b))return;if(game.pacts[pairKey(a,b)]){delete game.pacts[pairKey(a,b)];game.countries[a].stability=clamp(game.countries[a].stability-8,0,100)}game.wars[pairKey(a,b)]=true;setRelation(a,b,-100);game.countries[a].warSupport=clamp(game.countries[a].warSupport+5,0,100);game.countries[b].warSupport=clamp(game.countries[b].warSupport+8,0,100);if(playerInitiated)game.countries[a].stability=clamp(game.countries[a].stability-5,0,100);log(`${game.countries[a].name}向${game.countries[b].name}宣战。`,'bad');if(playerInitiated){renderDiplomacy(document.getElementById('drawer'));renderAll();toast('战争已经开始','bad')}}
function offerPeace(code){const my=score(game.player),their=score(code),chance=clamp(45+(my-their)*1.2,15,85);if(Math.random()*100<chance){delete game.wars[pairKey(game.player,code)];setRelation(game.player,code,-25);currentCountry().stability=clamp(currentCountry().stability+3,0,100);log(`${currentCountry().short}与${game.countries[code].short}达成停战。`,'good');toast('对方接受了停战','good')}else toast('对方拒绝停战','bad');renderDiplomacy(document.getElementById('drawer'));renderAll()}
function renderLog(d){d.innerHTML=drawerHead('战报与国家日志')+`<div class="logList">${game.log.map(x=>`<div class="logItem ${x.type||''}"><time>${x.date}</time>${esc(x.text)}</div>`).join('')||'暂无记录'}</div>`}
function log(text,type=''){if(!game)return;game.log.unshift({date:`${game.year}.${game.month}`,text,type});game.log=game.log.slice(0,120)}

function showTutorial(){if(game.tutorialShown)return;game.tutorialShown=true;showModal(`<h2>统帅入门</h2><p>这是一个按月推进的大战略游戏。你不需要精确操作大量部队，重点是长期规划。</p><div class="tutorialStep"><b>1. 点选省份</b><span>点击地图区域，在右侧查看驻军、工业、要塞和可用行动。</span></div><div class="tutorialStep"><b>2. 建设与招募</b><span>工业会增加每月收入；要塞提升防御；招募会消耗国库和人力。</span></div><div class="tutorialStep"><b>3. 科研与外交</b><span>使用底部菜单研究科技、改善关系、签署条约或宣战。</span></div><div class="tutorialStep"><b>4. 推进月份</b><span>点击右上角“结束本月”，经济结算、科研推进、AI 国家行动，并可能触发事件。</span></div><div class="choices"><button class="choice" onclick="hideModal()"><b>开始执政</b><span>建议先发展工业，再观察邻国局势。</span></button></div>`) }
functi