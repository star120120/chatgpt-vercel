on showModal(html){document.getElementById('modal').innerHTML=html;document.getElementById('modalBackdrop').classList.remove('hidden')}
function hideModal(){document.getElementById('modalBackdrop').classList.add('hidden');renderAll()}
function toast(msg,type=''){const t=document.getElementById('toast');t.textContent=msg;t.className=`toast ${type}`;clearTimeout(window._toast);window._toast=setTimeout(()=>t.classList.add('hidden'),1800)}
function checkGameOver(){if(game.gameOver)return;const pcount=countryProvinces(game.player).length;if(pcount===0){game.gameOver=true;showModal(`<h2>国家覆亡</h2><p>你的政府已经失去全部领土。历史并未按照你的计划发展。</p><div class="choices"><button class="choice" onclick="location.reload()"><b>返回主菜单</b></button></div>`)}else if(game.year>1945){game.gameOver=true;const s=score(game.player);showModal(`<h2>时代终章</h2><p>1945 年已经结束。你的最终胜利点为 <b>${s}</b>。</p><div class="choices"><button class="choice" onclick="location.reload()"><b>查看新的历史可能</b><span>${s>=120?'你已跻身世界强国。':'仍有许多战略可以重新尝试。'}</span></button></div>`)}}

// Events
renderCountryGrid();
document.getElementById('startBtn').onclick=newGame;document.getElementById('loadBtn').onclick=loadGame;document.getElementById('endTurnBtn').onclick=endTurn;document.getElementById('saveBtn').onclick=()=>saveGame(true);document.getElementById('tutorialBtn').onclick=()=>{game.tutorialShown=false;showTutorial()};document.getElementById('restartBtn').onclick=returnMenu;document.querySelectorAll('.navBtn').forEach(b=>b.onclick=()=>openDrawer(b.dataset.nav));document.getElementById('zoomIn').onclick=()=>zoom(.15);document.getElementById('zoomOut').onclick=()=>zoom(-.15);document.getElementById('zoomReset').onclick=()=>{transform={scale:1,x:0,y:0};applyTransform()};
const svg=document.getElementById('mapSvg');svg.addEventListener('pointerdown',e=>{if(e.target.closest('.province'))return;dragging=true;dragStart={x:e.clientX,y:e.clientY,ox:transform.x,oy:transform.y};svg.setPointerCapture(e.pointerId)});svg.addEventListener('pointermove',e=>{if(!dragging)return;transform.x=dragStart.ox+(e.clientX-dragStart.x)/transform.scale;transform.y=dragStart.oy+(e.clientY-dragStart.y)/transform.scale;applyTransform()});svg.addEventListener('pointerup',()=>dragging=false);svg.addEventListener('pointercancel',()=>dragging=false);
document.getElementById('modalBackdrop').addEventListener('click',e=>{if(e.target.id==='modalBackdrop')hideModal()});