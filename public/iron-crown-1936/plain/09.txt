e}已退出历史舞台。`,'bad')}}

function endTurn(){if(game.gameOver)return;actionMode=null;monthlyEconomy();aiTurns();progressResearch();advanceDate();timelineEvents();randomEvent();renderAll();saveGame(false);toast('新月份开始','good')}
function monthlyEconomy(){Object.entries(game.countries).forEach(([code,c])=>{if(!c.alive)return;const ind=countryIndustry(code),mult=1+c.tech.industry*.08;let income=(ind*1.55+countryProvinces(code).length*1.2)*mult;if(game.difficulty==='hard'&&code!==game.player)income*=1.12;c.money+=income;c.manpower+=Math.max(1,countryProvinces(code).length*.75);c.stability=clamp(c.stability+(c.money<0?-2:.15),0,100)})}
function aiTurns(){Object.entries(game.countries).forEach(([code,c])=>{if(code===game.player||!c.alive)return;aiDevelop(code);aiDiplomacy(code);aiWar(code)})}
function aiDevelop(code){const c=game.countries[code],ps=countryProvinces(code);if(!ps.length)return;if(c.money>58&&Math.random()<.5){const [id,p]=ps[Math.floor(Math.random()*ps.length)];if(p.industry<6){p.industry++;c.money-=35}}if(c.money>42&&c.manpower>18&&Math.random()<.65){const border=ps.filter(([,p])=>p.n.some(n=>game.provinces[n].owner!==code));const [id,p]=(border.length?border:ps)[Math.floor(Math.random()*(border.length||ps.length))];p.army+=2+c.tech.logistics;c.money-=26;c.manpower-=10}if(!c.activeResearch&&Math.random()<.55){const choices=Object.keys(TECHS).filter(t=>c.tech[t]<TECHS[t].max);if(choices.length){c.activeResearch=choices[Math.floor(Math.random()*choices.length)];c.researchProgress=0}}}
function aiDiplomacy(code){const c=game.countries[code];Object.keys(game.countries).forEach(other=>{if(other===code||!game.countries[other].alive)return;if(Math.random()<.04)setRelation(code,other,relation(code,other)+(Math.random()<.55?5:-5))});if(game.year>=1937&&Math.random()<.035*c.aggr){const targets=Object.keys(game.countries).filter(o=>o!==code&&game.countries[o].alive&&!isAtWar(code,o)&&relation(code,o)<-25&&hasBorder(code,o));if(targets.length){const t=targets[Math.floor(Math.random()*targets.length)];declareWar(code,t,false)}}}
function aiWar(code){const enemies=Object.keys(game.countries).filter(o=>isAtWar(code,o)&&game.countries[o].alive);if(!enemies.length)return;const attacks=[];countryProvinces(code).forEach(([id,p])=>p.n.forEach(n=>{const q=game.provinces[n];if(enemies.includes(q.owner)&&p.army>1)attacks.push([id,n])}));attacks.sort((x,y)=>game.provinces[y[0]