ntCountry().money-=15;if(currentCountry().activeResearch==='doctrine')currentCountry().researchProgress+=20;else currentCountry().warSupport+=2}],['暂缓改革','稳定度 +1',()=>{currentCountry().stability+=1}]]}
];
function randomEvent(){if(Math.random()>.22||game.gameOver)return;const e=EVENTS[Math.floor(Math.random()*EVENTS.length)];showModal(`<h2>${e.title}</h2><p>${e.text}</p><div class="choices">${e.choices.map((c,i)=>`<button class="choice" onclick="chooseEvent(${EVENTS.indexOf(e)},${i})"><b>${c[0]}</b><span>${c[1]}</span></button>`).join('')}</div>`)}
function chooseEvent(ei,ci){EVENTS[ei].choices[ci][2]();currentCountry().stability=clamp(currentCountry().stability,0,100);currentCountry().warSupport=clamp(currentCountry().warSupport,0,100);log(`事件“${EVENTS[ei].title}”：${EVENTS[ei].choices[ci][0]}。`);hideModal();renderAll()}

function openDrawer(type){document.querySelectorAll('.navBtn').forEach(b=>b.classList.toggle('active',b.dataset.nav===type));if(type==='map'){closeDrawer();return}const d=document.getElementById('drawer');d.classList.remove('hidden');if(type==='economy')renderEconomy(d);if(type==='research')renderResearch(d);if(type==='diplomacy')renderDiplomacy(d);if(type==='log')renderLog(d)}
function closeDrawer(){document.getElementById('drawer').classList.add('hidden');document.querySelectorAll('.navBtn').forEach(b=>b.classList.toggle('active',b.dataset.nav==='map'))}
function drawerHead(title){return `<div class="drawerHeader"><h3>${title}</h3><button class="closeBtn" onclick="closeDrawer()">×</button></div>`}
function renderEconomy(d){const c=currentCountry(),ind=countryIndustry(game.player),income=(ind*1.55+countryProvinces(game.player).length*1.2)*(1+c.tech.industry*.08);d.innerHTML=drawerHead('国家经济与行政')+`<div class="cardGrid"><div class="infoCard"><h4>月度财政</h4><p>工业和行政区域会持续提供收入。</p><div class="row"><span>预计月收入</span><b>+${fmt(income)}</b></div><div class="row"><span>当前国库</span><b>${fmt(c.money)}</b></div></div><div class="infoCard"><h4>工业基础</h4><p>工业决定收入，也是长期战争能力的核心。</p><div class="row"><span>总工业</span><b>${ind}</b></div><div class="row"><span>工业科技</span><b>${c.tech.industry}级</b></div></div><div class="infoCard"><h4>全国动员</h4><p>稳定度影响国家韧性，战争支持反映社会动员意愿。</p><div class="row"><span>稳定度</span><b>${fmt(c.stability)}%</b></div><div class="row"><span>战争支持</span><b>${fmt(c.warSupport)}%</b></div></div></div>`}
function renderResearch(d){const c=currentCoun