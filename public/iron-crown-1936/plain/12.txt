try();d.innerHTML=drawerHead('科技研究')+`<div class="cardGrid">${Object.entries(TECHS).map(([id,t])=>{const lvl=c.tech[id],active=c.activeResearch===id,cost=50+lvl*28;return `<div class="infoCard"><h4>${t.icon} ${t.name}</h4><p>${t.desc}</p><div class="row"><span>当前等级</span><b>${lvl}/${t.max}</b></div>${active?`<div class="row"><span>研究进度</span><b>${fmt(c.researchProgress)}/${cost}</b></div><button class="secondary tiny" style="width:100%;margin-top:9px" disabled>研究中</button>`:`<button class="secondary tiny" style="width:100%;margin-top:9px" ${lvl>=t.max||c.activeResearch?'disabled':''} onclick="startResearch('${id}')">${lvl>=t.max?'已完成':'开始研究'}</button>`}</div>`}).join('')}</div>`}
function startResearch(id){const c=currentCountry();if(c.activeResearch||c.tech[id]>=TECHS[id].max)return;c.activeResearch=id;c.researchProgress=0;log(`开始研究：${TECHS[id].name}。`,'good');renderResearch(document.getElementById('drawer'));renderAll()}
function renderDiplomacy(d){d.innerHTML=drawerHead('外交与战争')+`<div>${Object.entries(game.countries).filter(([k,c])=>k!==game.player&&c.alive).map(([k,c])=>diploRow(k,c)).join('')}</div>`}
function diploRow(k,c){const rel=relation(game.player,k),war=isAtWar(game.player,k),pact=game.pacts[pairKey(game.player,k)];return `<div class="diploRow"><div class="diploDot" style="--c:${c.color};background:${c.color}"></div><div><div class="diploName">${c.name}</div><div class="diploMeta">关系 ${rel} · ${war?'交战中':pact?'互不侵犯条约':'和平'}</div></div><div class="diploActions"><button class="secondary tiny" onclick="improveRelations('${k}')">改善关系</button>${war?`<button class="secondary tiny success" onclick="offerPeace('${k}')">议和</button>`:`<button class="secondary tiny" onclick="signPact('${k}')" ${rel<25||pact?'disabled':''}>签约</button><button class="secondary tiny danger" onclick="declareWar('${game.player}','${k}',true)">宣战</button>`}</div></div>`}
function openDiplomacy(code){openDrawer('diplomacy');setTimeout(()=>{const el=[...document.querySelectorAll('.diploName')].find(x=>x.textContent===game.countries[code].name);el?.scrollIntoView({block:'center'})},50)}
function improveRelations(code){const c=currentCountry();if(c.money<12){toast('国库不足','bad');return}c.money-=12;setRelation(game.player,code,relation(game.player,code)+15);log(`与${game.countries[code].short}的关系得到改善。`,'good');renderDiplomacy(document.getElementById('drawer'));renderAll()}
function sign